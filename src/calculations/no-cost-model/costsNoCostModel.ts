/**
 * Function for calculation of COSTS by NO COST MODEL
 * @params - no params
 * @returns the calculated value of total COSTS for no cost model; 
 * NOTE: current formula is just returnes zero as this model has no costs
 */

export const costsNoCostModel = () => 0;
